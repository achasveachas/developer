use #{@controller_class_name}use Newmodel4sControllerrequire './config/environment'

if ActiveRecord::Migrator.needs_migration?
  raise 'Migrations are pending. Run `rake db:migrate` to resolve the issue.'
end

run ApplicationController
use Newmodel17sController
use Newmodel13sControllerController
use NewmodelsControllerController
use Newmodel10sControllerController
use Newmodel9sControllerController
use Newmodel8sControllerController
use Newmodel7sControllerControllerrun Newmodel2sControllerrun NewmodelsController
use Newmodel3sController